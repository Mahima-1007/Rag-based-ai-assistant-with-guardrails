fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Redis
redis[hiredis]==5.0.4

# Qdrant
qdrant-client==1.9.1

# Embeddings
sentence-transformers>=2.7.0
torch

# LLM
groq==0.8.0

# LangChain (selective - loaders + splitters + prompt only)
langchain==0.2.1
langchain-community==0.2.1
pypdf==4.2.0
docx2txt==0.8

# BM25
rank-bm25==0.2.2

# Reranking
sentence-transformers==2.7.0  # CrossEncoder is part of sentence-transformers

# PII Detection (Disabled due to missing C++ compilers on Windows for this Python version)
# Monitoring
structlog==24.1.0

# Utilities
httpx==0.27.0
python-dotenv==1.0.1
aiofiles==23.2.1
